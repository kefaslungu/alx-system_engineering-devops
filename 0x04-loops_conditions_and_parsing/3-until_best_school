#/usr/bin/env bash
#This script uses an `until` loop that runs as long as the value of the `count` variable is less than or equal to 10. 

count=1

until [ $count -gt 10 ]
do
  echo "Best School"
  count=$((count + 1))
done
